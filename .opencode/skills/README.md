# Skills (OpenCode)

Canonical: `.github/skills/*/SKILL.md`. This tree uses the same folder layout.

| Skill | Purpose |
|-------|---------|
| `fermentflow-foundations` | DDD layout, branch evolution |
| `ci-checks` | Local CI-aligned commands |
| `docs-verification` | Doc consistency and links |
| `workspace-review` | Full repo audit |
| `e2e-testing` | Docker + build/test smoke |

Resync from `.github/skills/` when skills change.
