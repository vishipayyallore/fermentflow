# Architecture Decision Records (compatibility)

This folder is a compatibility shim for older links that referenced `docs/adr/`.
The canonical ADR files now live under `docs/02-adr/`.

Use the ADR index here: [ADR index](../02-adr/README.md).

If you maintain documentation references, prefer `docs/02-adr/` paths.
